# Copy this file to .env and add your credentials

# Reflect API Configuration
REFLECT_ACCESS_TOKEN=your_access_token_here

# Optional: Default graph ID
# REFLECT_DEFAULT_GRAPH_ID=your_graph_id

# Optional: OAuth credentials (if not using access token)
# REFLECT_CLIENT_ID=your_client_id
# REFLECT_CLIENT_SECRET=your_client_secret
