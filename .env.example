# Reflect API OAuth2 credentials
# Get these from https://reflect.app/oauth
REFLECT_CLIENT_ID=your_client_id_here
REFLECT_CLIENT_SECRET=your_client_secret_here

# Optional: OAuth2 redirect URI (defaults to http://localhost:8080/callback)
# REFLECT_REDIRECT_URI=http://localhost:8080/callback

# Optional: Default graph ID to use for operations
# REFLECT_DEFAULT_GRAPH_ID=your_default_graph_id

# Optional: If you already have an access token
# REFLECT_ACCESS_TOKEN=your_access_token
# REFLECT_REFRESH_TOKEN=your_refresh_token